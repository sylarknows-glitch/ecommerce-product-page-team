server {
listen 80;
server_name localhost;
# Directory where the static files are
root /usr/share/nginx/html;
index index.html index.htm;
# SPA fallback: if a file is not found, serve index.html
location / {
try_files $uri $uri/ /index.html;
}
}
